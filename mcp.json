{
  "mcpServers": {
    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://postgres:postgres@localhost:51214/template1"
      ],
      "description": "ShortsHub 데이터베이스(Prisma PostgreSQL)에 직접 접근하여 쿼리하고 스키마를 읽어오는 MCP 서버입니다."
    },
    "puppeteer": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-puppeteer"
      ],
      "description": "YouTube, TikTok 등 웹 페이지를 자동화 브라우저로 직접 스크래핑하여 트렌드를 분석하는 MCP 서버입니다."
    },
    "brave-search": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-brave-search"
      ],
      "env": {
        "BRAVE_API_KEY": "YOUR_BRAVE_API_KEY_HERE"
      },
      "description": "최신 인터넷 검색을 수행하여 AI가 실시간 트렌드 정보를 가져오도록 돕는 브레이브 서치 MCP 서버입니다."
    }
  }
}
